{% extends 'base.html.twig' %}
{% block body %}
    <h1>Create a category</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
<button>{{ button_label|default('Add category') }}</button>
{{ form_end(form) }}
{% endblock %}
